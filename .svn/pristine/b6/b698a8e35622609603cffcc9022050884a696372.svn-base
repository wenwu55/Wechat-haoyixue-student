<!--pages/homeworkDetail/homeworkDetail.wxml-->
<view class="main">
  <view class="title">{{title}}</view>
  <view class="time">{{createTime}}</view>
  <view class="content">{{content}}</view>
  <image class="pics" wx:for="{{imageList}}" wx:key="{{index}}" src="{{item}}"></image>
</view>
